# NoteSpace 笔记应用后端开发清单

## 一、技术栈

### 后端技术栈
- **语言**: Java 8
- **框架**: Spring Boot 2.x
- **ORM**: MyBatis Plus
- **数据库**: MySQL 8.0+
- **认证**: JWT (JSON Web Token)
- **其他**:
  - Redis (可选，用于缓存和会话管理)
  - Maven/Gradle (项目构建)

### 前端技术栈
- Vue 3 + TypeScript
- Vite
- Tailwind CSS

---

## 二、数据库设计

### 2.1 用户表 (t_user)

```sql
CREATE TABLE t_user (
  id BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '用户ID',
  username VARCHAR(50) NOT NULL COMMENT '用户名',
  email VARCHAR(100) NOT NULL UNIQUE COMMENT '邮箱',
  password VARCHAR(255) NOT NULL COMMENT '密码(加密)',
  avatar VARCHAR(500) COMMENT '头像URL',
  status TINYINT DEFAULT 1 COMMENT '状态(1:正常 0:禁用)',
  create_time DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  update_time DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  INDEX idx_email (email),
  INDEX idx_username (username)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='用户表';
```

### 2.2 笔记本表 (t_notebook)

```sql
CREATE TABLE t_notebook (
  id BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '笔记本ID',
  user_id BIGINT NOT NULL COMMENT '用户ID',
  name VARCHAR(100) NOT NULL COMMENT '笔记本名称',
  icon VARCHAR(50) DEFAULT 'folder' COMMENT '图标名称',
  color VARCHAR(20) DEFAULT '#3b82f6' COMMENT '图标颜色',
  sort_order INT DEFAULT 0 COMMENT '排序序号',
  create_time DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  update_time DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  INDEX idx_user_id (user_id),
  FOREIGN KEY (user_id) REFERENCES t_user(id) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='笔记本表';
```

### 2.3 笔记表 (t_note)

```sql
CREATE TABLE t_note (
  id BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '笔记ID',
  user_id BIGINT NOT NULL COMMENT '用户ID',
  notebook_id BIGINT NOT NULL COMMENT '笔记本ID',
  title VARCHAR(500) DEFAULT '' COMMENT '标题',
  content TEXT COMMENT '内容',
  preview TEXT COMMENT '预览文本(前100字)',
  is_pinned TINYINT DEFAULT 0 COMMENT '是否固定(1:是 0:否)',
  is_shared TINYINT DEFAULT 0 COMMENT '是否共享(1:是 0:否)',
  create_time DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  update_time DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  INDEX idx_user_id (user_id),
  INDEX idx_notebook_id (notebook_id),
  INDEX idx_update_time (update_time),
  INDEX idx_is_pinned (is_pinned),
  FULLTEXT INDEX ft_title_content (title, content) WITH PARSER ngram,
  FOREIGN KEY (user_id) REFERENCES t_user(id) ON DELETE CASCADE,
  FOREIGN KEY (notebook_id) REFERENCES t_notebook(id) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='笔记表';
```

### 2.4 标签表 (t_tag)

```sql
CREATE TABLE t_tag (
  id BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '标签ID',
  user_id BIGINT NOT NULL COMMENT '用户ID',
  name VARCHAR(50) NOT NULL COMMENT '标签名称',
  create_time DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  UNIQUE KEY uk_user_name (user_id, name),
  INDEX idx_user_id (user_id),
  FOREIGN KEY (user_id) REFERENCES t_user(id) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='标签表';
```

### 2.5 笔记标签关联表 (t_note_tag)

```sql
CREATE TABLE t_note_tag (
  id BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT 'ID',
  note_id BIGINT NOT NULL COMMENT '笔记ID',
  tag_id BIGINT NOT NULL COMMENT '标签ID',
  create_time DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  UNIQUE KEY uk_note_tag (note_id, tag_id),
  INDEX idx_note_id (note_id),
  INDEX idx_tag_id (tag_id),
  FOREIGN KEY (note_id) REFERENCES t_note(id) ON DELETE CASCADE,
  FOREIGN KEY (tag_id) REFERENCES t_tag(id) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='笔记标签关联表';
```

---

## 三、API 接口定义

> **重要约定**：
> 1. 所有接口只使用 **GET** 和 **POST** 两种请求方法
> 2. GET 请求参数以查询字符串形式传递：`?key=value`
> 3. POST 请求使用 JSON 格式传递参数
> 4. 所有接口返回统一格式的 JSON 数据
> 5. 需要认证的接口需要在请求头中携带 JWT Token：`Authorization: Bearer {token}`

### 3.1 统一响应格式

```json
{
  "code": 200,
  "message": "success",
  "data": {},
  "timestamp": 1234567890
}
```

### 3.2 认证相关接口

#### 用户注册
```
POST /api/auth/register
```

**请求参数**：
```json
{
  "username": "用户名",
  "email": "user@example.com",
  "password": "123456"
}
```

**响应**：
```json
{
  "code": 200,
  "message": "注册成功",
  "data": {
    "userId": 1,
    "username": "用户名",
    "email": "user@example.com",
    "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."
  }
}
```

#### 用户登录
```
POST /api/auth/login
```

**请求参数**：
```json
{
  "email": "user@example.com",
  "password": "123456"
}
```

**响应**：
```json
{
  "code": 200,
  "message": "登录成功",
  "data": {
    "userId": 1,
    "username": "用户名",
    "email": "user@example.com",
    "avatar": "头像URL",
    "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."
  }
}
```

#### 用户登出
```
POST /api/auth/logout
```

**请求头**：
```
Authorization: Bearer {token}
```

**响应**：
```json
{
  "code": 200,
  "message": "登出成功"
}
```

#### 获取当前用户信息
```
GET /api/auth/info?token={token}
```

**响应**：
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "userId": 1,
    "username": "用户名",
    "email": "user@example.com",
    "avatar": "头像URL",
    "createTime": "2024-01-01 12:00:00"
  }
}
```

#### 刷新 Token
```
POST /api/auth/refresh
```

**请求参数**：
```json
{
  "refreshToken": "刷新令牌"
}
```

**响应**：
```json
{
  "code": 200,
  "message": "刷新成功",
  "data": {
    "token": "新的访问令牌"
  }
}
```

### 3.3 笔记本接口

#### 获取所有笔记本
```
GET /api/notebook/list?token={token}
```

**响应**：
```json
{
  "code": 200,
  "message": "success",
  "data": [
    {
      "id": 1,
      "name": "产品设计",
      "icon": "folder",
      "color": "#f59e0b",
      "sortOrder": 0,
      "createTime": "2024-01-01 12:00:00",
      "noteCount": 5
    }
  ]
}
```

#### 创建笔记本
```
POST /api/notebook/create
```

**请求参数**：
```json
{
  "token": "xxx",
  "name": "新笔记本",
  "icon": "folder",
  "color": "#3b82f6"
}
```

**响应**：
```json
{
  "code": 200,
  "message": "创建成功",
  "data": {
    "id": 1,
    "name": "新笔记本",
    "icon": "folder",
    "color": "#3b82f6"
  }
}
```

#### 更新笔记本
```
POST /api/notebook/update
```

**请求参数**：
```json
{
  "token": "xxx",
  "id": 1,
  "name": "更新后的名称",
  "icon": "work",
  "color": "#10b981"
}
```

**响应**：
```json
{
  "code": 200,
  "message": "更新成功"
}
```

#### 删除笔记本
```
POST /api/notebook/delete
```

**请求参数**：
```json
{
  "token": "xxx",
  "id": 1
}
```

**响应**：
```json
{
  "code": 200,
  "message": "删除成功"
}
```

#### 获取笔记本详情
```
GET /api/notebook/detail?token={token}&id={id}
```

**响应**：
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "id": 1,
    "name": "产品设计",
    "icon": "folder",
    "color": "#f59e0b",
    "createTime": "2024-01-01 12:00:00",
    "notes": [
      {
        "id": 1,
        "title": "笔记标题",
        "preview": "预览内容...",
        "updateTime": "2024-01-01 12:00:00"
      }
    ]
  }
}
```

### 3.4 笔记接口

#### 获取笔记列表
```
GET /api/note/list?token={token}&notebookId={notebookId}&page={page}&size={size}
```

**查询参数**：
- `token`: JWT Token (必填)
- `notebookId`: 笔记本ID (可选，不传则查询所有)
- `page`: 页码，默认1
- `size`: 每页数量，默认20

**响应**：
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "total": 100,
    "page": 1,
    "size": 20,
    "list": [
      {
        "id": 1,
        "notebookId": 1,
        "title": "笔记标题",
        "preview": "预览内容...",
        "tags": ["工作", "API"],
        "isPinned": true,
        "updateTime": "刚刚"
      }
    ]
  }
}
```

#### 获取笔记详情
```
GET /api/note/detail?token={token}&id={id}
```

**响应**：
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "id": 1,
    "notebookId": 1,
    "title": "笔记标题",
    "content": "笔记内容...",
    "tags": ["工作", "API"],
    "isPinned": true,
    "isShared": false,
    "createTime": "2024-01-01 12:00:00",
    "updateTime": "2024-01-01 12:00:00"
  }
}
```

#### 创建笔记
```
POST /api/note/create
```

**请求参数**：
```json
{
  "token": "xxx",
  "notebookId": 1,
  "title": "新笔记标题",
  "content": "笔记内容",
  "tags": ["工作", "API"]
}
```

**响应**：
```json
{
  "code": 200,
  "message": "创建成功",
  "data": {
    "id": 1,
    "title": "新笔记标题",
    "updateTime": "刚刚"
  }
}
```

#### 更新笔记
```
POST /api/note/update
```

**请求参数**：
```json
{
  "token": "xxx",
  "id": 1,
  "title": "更新后的标题",
  "content": "更新后的内容",
  "tags": ["工作", "API", "设计"]
}
```

**响应**：
```json
{
  "code": 200,
  "message": "更新成功",
  "data": {
    "id": 1,
    "updateTime": "刚刚"
  }
}
```

#### 删除笔记
```
POST /api/note/delete
```

**请求参数**：
```json
{
  "token": "xxx",
  "id": 1
}
```

**响应**：
```json
{
  "code": 200,
  "message": "删除成功"
}
```

#### 切换笔记固定状态
```
POST /api/note/togglePin
```

**请求参数**：
```json
{
  "token": "xxx",
  "id": 1
}
```

**响应**：
```json
{
  "code": 200,
  "message": "操作成功",
  "data": {
    "id": 1,
    "isPinned": true
  }
}
```

### 3.5 搜索接口

#### 全文搜索笔记
```
GET /api/search/note?token={token}&keyword={keyword}&page={page}&size={size}
```

**查询参数**：
- `token`: JWT Token (必填)
- `keyword`: 搜索关键词 (必填)
- `page`: 页码，默认1
- `size`: 每页数量，默认20

**响应**：
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "total": 10,
    "keyword": "API",
    "list": [
      {
        "id": 1,
        "notebookId": 1,
        "title": "阿波罗项目接口规范",
        "preview": "定义新空间探索模块仪表盘的核心架构...",
        "tags": ["工程", "API"],
        "updateTime": "刚刚"
      }
    ]
  }
}
```

#### 搜索标签
```
GET /api/search/tag?token={token}&keyword={keyword}
```

**响应**：
```json
{
  "code": 200,
  "message": "success",
  "data": [
    {
      "id": 1,
      "name": "工作",
      "noteCount": 15
    }
  ]
}
```

#### 按标签筛选笔记
```
GET /api/note/byTag?token={token}&tagId={tagId}&page={page}&size={size}
```

**响应**：
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "total": 5,
    "tagId": 1,
    "tagName": "工作",
    "list": [
      {
        "id": 1,
        "title": "笔记标题",
        "preview": "预览内容...",
        "updateTime": "刚刚"
      }
    ]
  }
}
```

### 3.6 标签接口

#### 获取所有标签
```
GET /api/tag/list?token={token}
```

**响应**：
```json
{
  "code": 200,
  "message": "success",
  "data": [
    {
      "id": 1,
      "name": "工作",
      "noteCount": 15,
      "createTime": "2024-01-01 12:00:00"
    },
    {
      "id": 2,
      "name": "API",
      "noteCount": 8,
      "createTime": "2024-01-01 12:00:00"
    }
  ]
}
```

#### 创建标签
```
POST /api/tag/create
```

**请求参数**：
```json
{
  "token": "xxx",
  "name": "新标签"
}
```

**响应**：
```json
{
  "code": 200,
  "message": "创建成功",
  "data": {
    "id": 1,
    "name": "新标签"
  }
}
```

#### 删除标签
```
POST /api/tag/delete
```

**请求参数**：
```json
{
  "token": "xxx",
  "id": 1
}
```

**响应**：
```json
{
  "code": 200,
  "message": "删除成功"
}
```

---

## 四、JWT 认证方案

### 4.1 JWT Token 结构

```
Header.Payload.Signature
```

**Header（头部）**：
```json
{
  "alg": "HS256",
  "typ": "JWT"
}
```

**Payload（负载）**：
```json
{
  "userId": 1,
  "email": "user@example.com",
  "iat": 1234567890,
  "exp": 1234567890
}
```

### 4.2 Token 生成策略

- **Access Token（访问令牌）**：
  - 有效期：2小时
  - 用途：访问需要认证的 API

- **Refresh Token（刷新令牌）**：
  - 有效期：7天
  - 用途：刷新 Access Token

### 4.3 Token 存储方式

**前端存储**：
```
localStorage.setItem('token', accessToken)
localStorage.setItem('refreshToken', refreshToken)
```

**请求头携带**：
```
Authorization: Bearer {accessToken}
```

### 4.4 Token 刷新流程

1. 前端发起请求，携带 Access Token
2. 后端验证 Token，如果过期返回 401
3. 前端收到 401，使用 Refresh Token 调用刷新接口
4. 后端验证 Refresh Token，返回新的 Access Token
5. 前端更新本地存储，重新发起原请求

### 4.5 Token 安全建议

1. 使用 HTTPS 传输
2. 设置合理的过期时间
3. 敏感操作二次验证
4. 定期刷新 Token
5. 登出时清除客户端 Token

---

## 五、前端功能清单

### 5.1 登录页面
- [x] 邮箱/密码登录
- [x] 记住我功能（持久化 Token）
- [x] 表单验证
- [ ] 注册功能
- [ ] 忘记密码功能

### 5.2 侧边栏
- [x] 显示所有笔记本
- [x] 创建笔记本
- [x] 编辑笔记本（名称、图标、颜色）
- [x] 删除笔记本（需检查是否为空）
- [x] 显示所有标签
- [x] 点击标签筛选笔记
- [x] 新建笔记按钮

### 5.3 笔记列表
- [x] 显示笔记列表
- [x] 搜索笔记
- [x] 视图筛选（全部/已固定/已共享）
- [x] 点击笔记进入编辑
- [x] 删除笔记
- [x] 显示固定图标
- [x] 显示更新时间

### 5.4 编辑器
- [x] 标题编辑
- [x] 内容编辑（Markdown）
- [x] 标签管理（添加、删除、智能建议）
- [x] 固定/取消固定
- [x] 删除笔记
- [x] 显示更新时间
- [x] 用户菜单（登出）

### 5.5 响应式设计
- [x] 桌面端三栏布局
- [x] 移动端单栏切换

---

## 六、开发待办事项

### 后端开发
- [ ] 搭建 Spring Boot 项目框架
- [ ] 配置 MyBatis Plus
- [ ] 设计并创建数据库表
- [ ] 实现 JWT 认证机制
- [ ] 实现用户注册/登录接口
- [ ] 实现笔记本 CRUD 接口
- [ ] 实现笔记 CRUD 接口
- [ ] 实现标签管理接口
- [ ] 实现全文搜索功能
- [ ] 添加接口文档（Swagger/Knife4j）
- [ ] 单元测试和集成测试

### 前端对接
- [ ] 封装 Axios 请求拦截器（自动添加 Token）
- [ ] 封装响应拦截器（处理 401 跳转登录）
- [ ] 实现 Token 刷新机制
- [ ] 对接登录接口
- [ ] 对接笔记本接口
- [ ] 对接笔记接口
- [ ] 对接搜索接口
- [ ] 错误处理和提示

### 优化和部署
- [ ] 性能优化
- [ ] 安全加固
- [ ] 日志记录
- [ ] Docker 容器化
- [ ] CI/CD 配置

---

## 七、附录

### 7.1 状态码定义

| 状态码 | 说明 |
|--------|------|
| 200 | 请求成功 |
| 400 | 请求参数错误 |
| 401 | 未认证或 Token 过期 |
| 403 | 无权限访问 |
| 404 | 资源不存在 |
| 500 | 服务器内部错误 |

### 7.2 常用图标列表

```
folder, work, home, person, school,
favorite, rocket, lightbulb, description,
search, edit, delete, add, close, menu,
arrow_back, push_pin, share, logout, etc.
```

### 7.3 预设颜色列表

```
#ef4444 (红色)
#f59e0b (橙色)
#10b981 (绿色)
#3b82f6 (蓝色)
#8b5cf6 (紫色)
#ec4899 (粉色)
#64748b (灰色)
#0f172a (深色)
```

---

**文档版本**: v1.0
**创建日期**: 2024-01-26
**最后更新**: 2024-01-26
